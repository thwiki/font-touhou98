<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ fontName }}</title>
		{{ meta | safe }}
    <style type="text/css">
      body {
        margin: 0;
        padding:10px 20px;
				background: black;
				color: white;
        font-family:"Helvetica Neue", Arial, sans-serif;
      }
			a,
			a:visited,
			a:active,
			a:hover {
				color: white;
			}
			nav {
				display: flex;
				flex-wrap: wrap;
				justify-content: center;
			}
			nav a {
				display: block;
				margin: 0.5em;
			}
      h1 {
				margin: 40px 0;
				font-size: 48px;
				text-align: center;
      }
      h2 {
        margin:0 0 20px;
        font-size: 32px;
				text-align: center;
      }
      .icons {
        margin-bottom: 40px;
				display: flex;
				flex-wrap: wrap;
  		  justify-content: center;
      }
      .icons__item {
				padding: 8px;
      }
      .icons__item,
      .icons__item i {
        cursor: pointer;
        vertical-align: middle;
				text-align: center;
      }
      .icons__item:hover {
        color: #3c90be;
      }
      .icons__item span {
        display: inline-block;
        line-height: 3em;
        margin-left: 5px;
        white-space: nowrap;
      }
    </style>
		<link rel="stylesheet" href="https://fastly.jsdelivr.net/npm/font-touhou98/dist/touhou98.css" />
  </head>
  <body>
		<nav>
			<a target="_blank" href="https://github.com/thwiki/font-touhou98">Github</a>
			<a target="_blank" href="https://www.npmjs.com/package/font-touhou98">npm</a>
			<a target="_blank" href="http://www16.big.or.jp/~zun/">上海アリス幻樂団</a>
			<a target="_blank" href="https://thwiki.cc">THBWiki</a>
		</nav>

		<h1 class="{{ className }}" contenteditable="plaintext-only">TOUHOU98 FONT</h1>
    <h2>Glyphs List</h2>
		<div class="icons" id="icon-glyphs">
			{% for glyph in glyphs %}
				<div class="icons__item">
					<i class="{{ className }} {{ className }}-2x">{{ glyph.unicode[1] }}</i><br />
					<span>{{ glyph.unicode[1] }}</span>
				</div>
			{% endfor %}
		</div>
    <h2>Ligatures List</h2>
		<div class="icons" id="icon-ligatures">
			{% for ligature in ligatures %}
				<div class="icons__item">
					<i class="{{ className }} {{ className }}-2x">{{ ligature.unicode[1] }}</i><br />
					<span>{{ ligature.unicode[1] }}</span>
				</div>
			{% endfor %}
		</div>
  </body>
</html>